apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "core-payment-solution.serviceAccountName" . }}
  labels:
    {{- include "core-payment-solution.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
